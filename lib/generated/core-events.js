// AUTO-GENERATED by autogen/generate-events.js — do not edit by hand.
// Source: autogen/proto/xproto.xml
'use strict';

function parseKeyPress(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'KeyPress' };
  event.keycode = code;
  event.time = extra;
  event.root = raw.readUInt32LE(0);
  event.wid = raw.readUInt32LE(4);
  event.child = raw.readUInt32LE(8);
  event.rootx = raw.readInt16LE(12);
  event.rooty = raw.readInt16LE(14);
  event.x = raw.readInt16LE(16);
  event.y = raw.readInt16LE(18);
  event.buttons = raw.readUInt16LE(20);
  event.sameScreen = raw.readUInt8(22);
  return event;
}

function parseKeyRelease(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'KeyRelease' };
  event.keycode = code;
  event.time = extra;
  event.root = raw.readUInt32LE(0);
  event.wid = raw.readUInt32LE(4);
  event.child = raw.readUInt32LE(8);
  event.rootx = raw.readInt16LE(12);
  event.rooty = raw.readInt16LE(14);
  event.x = raw.readInt16LE(16);
  event.y = raw.readInt16LE(18);
  event.buttons = raw.readUInt16LE(20);
  event.sameScreen = raw.readUInt8(22);
  return event;
}

function parseButtonPress(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'ButtonPress' };
  event.keycode = code;
  event.time = extra;
  event.root = raw.readUInt32LE(0);
  event.wid = raw.readUInt32LE(4);
  event.child = raw.readUInt32LE(8);
  event.rootx = raw.readInt16LE(12);
  event.rooty = raw.readInt16LE(14);
  event.x = raw.readInt16LE(16);
  event.y = raw.readInt16LE(18);
  event.buttons = raw.readUInt16LE(20);
  event.sameScreen = raw.readUInt8(22);
  return event;
}

function parseButtonRelease(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'ButtonRelease' };
  event.keycode = code;
  event.time = extra;
  event.root = raw.readUInt32LE(0);
  event.wid = raw.readUInt32LE(4);
  event.child = raw.readUInt32LE(8);
  event.rootx = raw.readInt16LE(12);
  event.rooty = raw.readInt16LE(14);
  event.x = raw.readInt16LE(16);
  event.y = raw.readInt16LE(18);
  event.buttons = raw.readUInt16LE(20);
  event.sameScreen = raw.readUInt8(22);
  return event;
}

function parseMotionNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'MotionNotify' };
  event.keycode = code;
  event.time = extra;
  event.root = raw.readUInt32LE(0);
  event.wid = raw.readUInt32LE(4);
  event.child = raw.readUInt32LE(8);
  event.rootx = raw.readInt16LE(12);
  event.rooty = raw.readInt16LE(14);
  event.x = raw.readInt16LE(16);
  event.y = raw.readInt16LE(18);
  event.buttons = raw.readUInt16LE(20);
  event.sameScreen = raw.readUInt8(22);
  return event;
}

function parseEnterNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'EnterNotify' };
  event.detail = code;
  event.time = extra;
  event.root = raw.readUInt32LE(0);
  event.wid = raw.readUInt32LE(4);
  event.child = raw.readUInt32LE(8);
  event.rootx = raw.readInt16LE(12);
  event.rooty = raw.readInt16LE(14);
  event.x = raw.readInt16LE(16);
  event.y = raw.readInt16LE(18);
  event.buttons = raw.readUInt16LE(20);
  event.mode = raw.readUInt8(22);
  event.sameScreenFocus = raw.readUInt8(23);
  event.values = [event.root, event.wid, event.child, event.rootx, event.rooty, event.x, event.y, event.buttons, event.mode];
  return event;
}

function parseLeaveNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'LeaveNotify' };
  event.detail = code;
  event.time = extra;
  event.root = raw.readUInt32LE(0);
  event.wid = raw.readUInt32LE(4);
  event.child = raw.readUInt32LE(8);
  event.rootx = raw.readInt16LE(12);
  event.rooty = raw.readInt16LE(14);
  event.x = raw.readInt16LE(16);
  event.y = raw.readInt16LE(18);
  event.buttons = raw.readUInt16LE(20);
  event.mode = raw.readUInt8(22);
  event.sameScreenFocus = raw.readUInt8(23);
  event.values = [event.root, event.wid, event.child, event.rootx, event.rooty, event.x, event.y, event.buttons, event.mode];
  return event;
}

function parseFocusIn(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'FocusIn' };
  event.detail = code;
  event.wid = extra;
  event.mode = raw.readUInt8(0);
  return event;
}

function parseFocusOut(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'FocusOut' };
  event.detail = code;
  event.wid = extra;
  event.mode = raw.readUInt8(0);
  return event;
}

function parseKeymapNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, name: 'KeymapNotify' };
  event.keys = Buffer.concat([headerBuf.slice(1, 8), raw]);
  return event;
}

function parseExpose(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'Expose' };
  event.wid = extra;
  event.x = raw.readUInt16LE(0);
  event.y = raw.readUInt16LE(2);
  event.width = raw.readUInt16LE(4);
  event.height = raw.readUInt16LE(6);
  event.count = raw.readUInt16LE(8);
  return event;
}

function parseGraphicsExposure(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'GraphicsExposure' };
  event.drawable = extra;
  event.x = raw.readUInt16LE(0);
  event.y = raw.readUInt16LE(2);
  event.width = raw.readUInt16LE(4);
  event.height = raw.readUInt16LE(6);
  event.minorOpcode = raw.readUInt16LE(8);
  event.count = raw.readUInt16LE(10);
  event.majorOpcode = raw.readUInt8(12);
  return event;
}

function parseNoExposure(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'NoExposure' };
  event.drawable = extra;
  event.minorOpcode = raw.readUInt16LE(0);
  event.majorOpcode = raw.readUInt8(2);
  return event;
}

function parseVisibilityNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'VisibilityNotify' };
  event.wid = extra;
  event.state = raw.readUInt8(0);
  return event;
}

function parseCreateNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'CreateNotify' };
  event.parent = extra;
  event.wid = raw.readUInt32LE(0);
  event.x = raw.readInt16LE(4);
  event.y = raw.readInt16LE(6);
  event.width = raw.readUInt16LE(8);
  event.height = raw.readUInt16LE(10);
  event.borderWidth = raw.readUInt16LE(12);
  event.overrideRedirect = !!raw.readUInt8(14);
  return event;
}

function parseDestroyNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'DestroyNotify' };
  event.event = extra;
  event.wid = raw.readUInt32LE(0);
  return event;
}

function parseUnmapNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'UnmapNotify' };
  event.event = extra;
  event.wid = raw.readUInt32LE(0);
  event.fromConfigure = !!raw.readUInt8(4);
  return event;
}

function parseMapNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'MapNotify' };
  event.event = extra;
  event.wid = raw.readUInt32LE(0);
  event.overrideRedirect = !!raw.readUInt8(4);
  return event;
}

function parseMapRequest(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'MapRequest' };
  event.parent = extra;
  event.wid = raw.readUInt32LE(0);
  return event;
}

function parseReparentNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'ReparentNotify' };
  event.event = extra;
  event.wid = raw.readUInt32LE(0);
  event.parent = raw.readUInt32LE(4);
  event.x = raw.readInt16LE(8);
  event.y = raw.readInt16LE(10);
  event.overrideRedirect = !!raw.readUInt8(12);
  return event;
}

function parseConfigureNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'ConfigureNotify' };
  event.wid = extra;
  event.wid1 = raw.readUInt32LE(0);
  event.aboveSibling = raw.readUInt32LE(4);
  event.x = raw.readInt16LE(8);
  event.y = raw.readInt16LE(10);
  event.width = raw.readUInt16LE(12);
  event.height = raw.readUInt16LE(14);
  event.borderWidth = raw.readUInt16LE(16);
  event.overrideRedirect = raw.readUInt8(18);
  return event;
}

function parseConfigureRequest(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'ConfigureRequest' };
  event.stackMode = code;
  event.parent = extra;
  event.wid = raw.readUInt32LE(0);
  event.sibling = raw.readUInt32LE(4);
  event.x = raw.readInt16LE(8);
  event.y = raw.readInt16LE(10);
  event.width = raw.readUInt16LE(12);
  event.height = raw.readUInt16LE(14);
  event.borderWidth = raw.readUInt16LE(16);
  event.mask = raw.readUInt16LE(18);
  return event;
}

function parseGravityNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'GravityNotify' };
  event.event = extra;
  event.wid = raw.readUInt32LE(0);
  event.x = raw.readInt16LE(4);
  event.y = raw.readInt16LE(6);
  return event;
}

function parseResizeRequest(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'ResizeRequest' };
  event.wid = extra;
  event.width = raw.readUInt16LE(0);
  event.height = raw.readUInt16LE(2);
  return event;
}

function parseCirculateNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'CirculateNotify' };
  event.event = extra;
  event.wid = raw.readUInt32LE(0);
  event.place = raw.readUInt8(8);
  return event;
}

function parseCirculateRequest(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'CirculateRequest' };
  event.event = extra;
  event.wid = raw.readUInt32LE(0);
  event.place = raw.readUInt8(8);
  return event;
}

function parsePropertyNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'PropertyNotify' };
  event.wid = extra;
  event.atom = raw.readUInt32LE(0);
  event.time = raw.readUInt32LE(4);
  event.state = raw.readUInt8(8);
  return event;
}

function parseSelectionClear(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'SelectionClear' };
  event.time = extra;
  event.owner = raw.readUInt32LE(0);
  event.selection = raw.readUInt32LE(4);
  return event;
}

function parseSelectionRequest(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'SelectionRequest' };
  event.time = extra;
  event.owner = raw.readUInt32LE(0);
  event.requestor = raw.readUInt32LE(4);
  event.selection = raw.readUInt32LE(8);
  event.target = raw.readUInt32LE(12);
  event.property = raw.readUInt32LE(16);
  return event;
}

function parseSelectionNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'SelectionNotify' };
  event.time = extra;
  event.requestor = raw.readUInt32LE(0);
  event.selection = raw.readUInt32LE(4);
  event.target = raw.readUInt32LE(8);
  event.property = raw.readUInt32LE(12);
  return event;
}

function parseColormapNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'ColormapNotify' };
  event.wid = extra;
  event.colormap = raw.readUInt32LE(0);
  event.new = raw.readUInt8(4);
  event.state = raw.readUInt8(5);
  return event;
}

function parseClientMessage(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'ClientMessage' };
  event.format = code;
  event.wid = extra;
  event.message_type = raw.readUInt32LE(0);
  const data = [];
  if (code === 32) {
    for (let i = 0; i < 5; i++) data.push(raw.readUInt32LE(4 + i * 4));
  } else if (code === 16) {
    for (let i = 0; i < 10; i++) data.push(raw.readUInt16LE(4 + i * 2));
  } else {
    for (let i = 0; i < 20; i++) data.push(raw.readUInt8(4 + i));
  }
  event.data = data;
  return event;
}

function parseMappingNotify(type, seq, extra, code, raw, headerBuf) {
  const event = { type: type & 0x7F, seq, name: 'MappingNotify' };
  event.request = headerBuf[4];
  event.firstKeyCode = headerBuf[5];
  event.count = headerBuf[6];
  return event;
}

const parsers = {
  2: parseKeyPress,
  3: parseKeyRelease,
  4: parseButtonPress,
  5: parseButtonRelease,
  6: parseMotionNotify,
  7: parseEnterNotify,
  8: parseLeaveNotify,
  9: parseFocusIn,
  10: parseFocusOut,
  11: parseKeymapNotify,
  12: parseExpose,
  13: parseGraphicsExposure,
  14: parseNoExposure,
  15: parseVisibilityNotify,
  16: parseCreateNotify,
  17: parseDestroyNotify,
  18: parseUnmapNotify,
  19: parseMapNotify,
  20: parseMapRequest,
  21: parseReparentNotify,
  22: parseConfigureNotify,
  23: parseConfigureRequest,
  24: parseGravityNotify,
  25: parseResizeRequest,
  26: parseCirculateNotify,
  27: parseCirculateRequest,
  28: parsePropertyNotify,
  29: parseSelectionClear,
  30: parseSelectionRequest,
  31: parseSelectionNotify,
  32: parseColormapNotify,
  33: parseClientMessage,
  34: parseMappingNotify,
};

function unpackEvent(type, seq, extra, code, raw, headerBuf) {
  const t = type & 0x7F;
  const fn = parsers[t];
  if (fn) return fn(t, seq, extra, code, raw, headerBuf);
  return { type: t, seq };
}

module.exports = { unpackEvent, parsers };
